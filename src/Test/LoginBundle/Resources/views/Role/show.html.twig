{% extends '::base.html.twig' %}
    
{% block body -%}
<div class="row">
    <div class="col-md-12">
        <div class="page-header-sector">
            <div class="row">
                <div class="col-md-12 breadcrumb-bar">
                    <ol class="breadcrumb">
                        <li><a href="{{ url('test_login_homepage')}}">Hompage</a></li>
                        <li><a href="{{ url('admin_role')}}">Rol</a></li>
                        <li class="active">Detalle Role</li>
                    </ol>								
                </div>
            </div>
        </div>
    </div>

    <div class="col-md-12">
        <div class="page-content-sector">
            <div class="row">
                <h1>Detalle Role</h1>
                <table class="records_list table table-bordered table-striped table-hover" border="1">
                    <tbody>
                        <tr>
                            <th>Id</th>
                            <td>{{ entity.id }}</td>
                        </tr>
                        <tr>
                            <th>Name</th>
                            <td>{{ entity.name }}</td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
        <div class="text-center">
            <div class="options-buttons-box inline">
                {{ form(delete_form) }}
                <a href="{{ path('admin_role_edit', { 'id': entity.id }) }}" class="btn btn-info">Modificar</a>
                <a href="{{ path('admin_role') }}" class="btn btn-default" role="button">Regresar</a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
{% block javascripts %}
{% endblock %}